<h2>Now Playing</h2>

<% @now_playing.first(15).each do |movie| %>
    <% if movie %>
        <a href="<%= url_for :controller => :movies, :action => :show, :id => movie['id'] %>">
            <%= image_tag("#{@configuration.base_url}w154#{movie['poster_path']}") if movie['poster_path'] %>
        </a>
    <% end %>
<% end %>