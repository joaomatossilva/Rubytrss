<h2>Top Rated</h2>

<% @top_rated.each do |movie| %>
    <% if movie %>
        <%= image_tag("#{@configuration.base_url}w154#{movie['poster_path']}") if movie['poster_path'] %>
    <% end %>
<% end %>